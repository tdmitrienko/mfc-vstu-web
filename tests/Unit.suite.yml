# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - Symfony:
            app_path: 'src'
            environment: 'test'
            kernel_class: '\App\Kernel'
        - Doctrine:
            depends: Symfony
            cleanup: true
