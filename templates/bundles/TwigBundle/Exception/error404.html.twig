{% extends 'bundles/TwigBundle/Exception/error.html.twig' %}

{% block title %}Страница не найдена{% endblock %}

{% block status_code %}404{% endblock %}

{% block message %}
    {% if app.environment is same as('dev') %}
        <div class="alert alert-danger text-start small mb-4">
            {{ message }}
        </div>
    {% else %}
        <p class="fs-4 text-secondary mb-4">
            {{ status_text|default('Похоже, такой страницы не существует.') }}
        </p>
    {% endif %}
{% endblock %}
