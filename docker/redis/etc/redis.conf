protected-mode no
port 6379
timeout 0
tcp-keepalive 300
daemonize no
logfile ""
databases 16

# 📦 Каталог для хранения данных и логов
dir /data

# 📸 Включаем RDB snapshots (дампы)
# Сохранять каждые 60 секунд, если было ≥1 изменение
save 60 1

# 🧹 Дополнительные настройки для стабильности
stop-writes-on-bgsave-error yes
rdbcompression yes
rdbchecksum yes
dbfilename dump.rdb
aof-use-rdb-preamble yes
